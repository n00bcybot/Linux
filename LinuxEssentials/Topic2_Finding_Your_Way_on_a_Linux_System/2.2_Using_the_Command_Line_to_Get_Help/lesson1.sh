# Getting help
# ----------------------------------------------------------------------------------------------------------------------

# Primary help source
man

# Another source
info

# Locating files
locate <file>

# Update locate database
updatedb

# locate command supports the usage of wildcards and regular expressions
# locate PATTERN is the same as locate *PATTERN*

# The "find" command
# find requires at least the path it should search
# expressions can be added to provide filter criteria for which files to display
# An example is the -name expression, which looks for files with a specific name

find . -name thesis.pdf

